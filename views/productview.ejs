<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Product</title>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/admin">TheCubes' Admin Page</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/addNew">Add New Product</a>
                </li>
                <li class="nav-item">
                      <a class="nav-link" href="/message">View Messages</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/">Back to Homepage</a>
                </li>
            </ul>
          </div>
        </div>
    </nav>
    
    <div class="container">
        <br>
        <div class="text-center"><h2>Product Details</h2></div>
        <br>
        <% if (product) { %>
            <img src= "/images/<%= product.image %>" class="float-start" width="400">
            <div style="padding: 15%;">
              <h3 style="right: 20;"><%= product.name %></h3>
              <h5>$<%= product.price.toFixed(2) %></h5>
              <a href="/editProduct/<%= product.cubeId %>" class="btn btn-primary">Edit Product</a> 
            </div>
        <% } else { %>
            <p>No product found.</p>
        <% } %>
        <a href="/admin" class="btn btn-secondary">Back</a>
    </div>
    
</body>
</html>