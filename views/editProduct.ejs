<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Edit Product</title>
    </head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/admin">TheCubes' Admin Page</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/addNew">Add New Product</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/message">View Messages</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/">Back to Homepage</a>
              </li>
            </ul>
          </div>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Update Product</h2></div>
        <br>
        <div class="form-control">
          <% if (product) { %> 
            <form action="/editProduct/<%= product.cubeId %>" method="POST" enctype="multipart/form-data">
              <div class="mb-3">  
                  <label for="name">Product Name:</label><br>
                  <input class="form-control" type="text" id="name" name="name" value = "<%= product.name %>" required><br>
              </div>

              <div class="mb-3">
                  <label for="quantity">Quantity:</label><br>
                  <input class="form-control" type="number" id="quantity" name="quantity" min="0" step="1" value = <%= product.quantity %> required><br>
              </div>

              <div class="mb-3">
                  <label for="price">Price:</label><br>
                  <input class="form-control" type="number" id="price" name="price" min="0" step="0.01" value = <%= product.price %> required><br>
              </div>

              <!--Piece of code breaks image displayed in website-->
              <!-- <div class="mb-3">
                  <label for="image">Current Image:</label><br>
                  <input class="form-control" type="text" name="currentImage" value = <%= product.image %> disabled><br>
                  <img src = "/images/<%= product.image %>" width="20%"><br><br>

                  New Image:<br> <input class="form-control" type="file" id="image" name="image" accept="image/*" value= <%= product.image %>><br>
              </div> -->

              <div class="mb-3">
                <label for="image">Image:</label><br>
                <img src = "/images/<%= product.image %>" width="20%"><br><br>
              </div>

              <button type="submit" class="btn btn-primary">Update Product</button>
            </form>
          <% } else { %>
              <p>No product found.</p>
          <% } %>
        </div>
      <br><a href ="/admin"><button type="button" class="btn btn-secondary">Back</button></a>
    </div>
</body>
</html>
