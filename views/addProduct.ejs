<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Add to Checkout</title>
    </head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">TheCubes</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/aboutUs">About Us</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/contactUs">Contact Us</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/login">Log-in</a>
              </li>
            </ul>
          </div>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Add to Checkout</h2></div>
        <br>
        <div class="form-control">
          <% if (product) { %> 
            <form action="/addProduct" method="POST" enctype="multipart/form-data">
              <div class="mb-3">  
                  <label for="name"><b>Product Name:</b></label><br>
                  <input type="text" id="name" name="name" class="form-control" value = "<%= product.name %>" disabled>
              </div>

              <div class="mb-3">
                  <label for="quantity"><b>Quantity:</b></label><br>
                  <input type="number" id="quantity" name="quantity" min="0" step="1" class="form-control" value = 1 required>
              </div>

              <div class="mb-3">
                  <label for="price"><b>Price:</b></label><br>
                  <input type="number" id="price" name="price" min="0" step="0.01" class="form-control" value = <%= product.price %> disabled>
              </div>

              <div class="mb-3">
                  <label for="image">Product:</label><br>
                  <img src = "/images/<%= product.image %>" width="20%">
              </div>
                <a href ="/payment/<%= product.cubeId %>"><button type="button" class="btn btn-primary">Checkout</button></a>
            </form>
          <% } else { %>
              <p>No product found.</p>
          <% } %>
        </div>
      <br><a href ="/product/<%= product.cubeId %>"><button type="button" class="btn btn-secondary">Back</button></a>
    </div>
</body>
</html>
